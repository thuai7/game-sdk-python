on:
  release:
    types:
      - published

jobs:
  zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: |
          zip -r sdk-python.zip src/*

      - uses: actions/upload-artifact@v2
        with:
          name: sdk-python-${{ github.sha }}.zip
          path: sdk-python.zip

  tar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - run: |
          tar -czvf sdk-python.tar.gz src/*

      - uses: actions/upload-artifact@v2
        with:
          name: sdk-python-${{ github.sha }}.tar.gz
          path: sdk-python.tar.gz

  release:
    needs:
      - zip
      - tar
    permissions:
      contents: write
    runs-on: ubuntu-latest
    strategy:
      matrix:
        extension:
          - zip
          - tar.gz
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v3
        with:
          name: sdk-python-${{ github.sha }}.${{ matrix.extension }}
          path: sdk-python

      - run: |
          cp CHANGELOG.md LICENSE README.md sdk-python/

      - name: Pack artifact (zip)
        if: startsWith(matrix.extension, 'zip')
        run: |
          zip -r sdk-python.zip sdk-python/*

      - name: Pack artifact (tar)
        if: startsWith(matrix.extension, 'tar')
        run: |
          tar -czvf sdk-python.tar.gz sdk-python/*

      - name: Upload artifact to release
        uses: softprops/action-gh-release@v1
        with:
          append_body: true
          files: |
            sdk-python.${{ matrix.extension }}
